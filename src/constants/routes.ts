import { BACKEND_HOST } from "./config";

export const LOGIN_URL = BACKEND_HOST + '/login';
export const REGISTER_URL = BACKEND_HOST + '/register';

export const CSRF_TOKEN_URL = BACKEND_HOST + '/csrftoken';
