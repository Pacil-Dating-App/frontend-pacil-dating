import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.ac.ui.cs.fedatingpacil',
  appName: 'frontend-pacil-dating',
  webDir: 'build'
};

export default config;
